name: Change Management Trigger

on:
  repository_dispatch:
    types: [change_update]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
